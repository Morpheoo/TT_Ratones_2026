# =============================================================
# Requirements para venv_310 (Python 3.10)
# SimBA + DeepLabCut + TensorFlow/Keras 2 (LSTM Grooming)
#
# Este venv NO usa GPU para inferencia. PyTorch va CPU-only
# (solo lo importa DeepLabCut, no se usa para predict).
# Las predicciones LSTM corren en TensorFlow/Keras 2 sobre CPU
# (modelo es 2.6 MB, irrelevante el speedup de GPU).
# =============================================================

--extra-index-url https://download.pytorch.org/whl/cpu

# PyTorch CPU-only (compatible con DeepLabCut import)
torch==2.7.1+cpu
torchvision==0.22.1+cpu

# DeepLabCut (entrenamiento DLC opcional + utilidades)
deeplabcut==2.3.11

# TensorFlow/Keras 2 (LSTM Grooming inferencia + entrenamiento)
# Pinear a 2.10.0 porque la LSTM productiva (grooming_lstm.keras) se
# entreno con esa version y no garantizamos compatibilidad cross-minor.
tensorflow==2.10.0
keras==2.10.0

# SimBA (Random Forest Grooming + Thigmotaxis)
simba-uw-tf-dev>=1.95.5

# Numerica e IO
numpy<2.0
scipy
pandas
scikit-learn
matplotlib
opencv-python
pillow
PyYAML
tqdm
imageio
imageio-ffmpeg

# Configuracion (este venv no toca Postgres; psycopg2/SQLAlchemy van en venv_311)
python-dotenv
